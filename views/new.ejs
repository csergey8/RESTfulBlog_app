<% include partials/header %>
    <div class="ui main container segment">
        <div class="huge header">New Blog</div>
        <form class="ui form" action="/blogs" method="post">
            <div class="field">
                <label>Title</label>
                <input type="text" name="blog[title]" placeholder="title" class="">
            </div>
            <div class="field">
                <label>Image</label>
                <input type="text" name="blog[image]" placeholder="url" class="">
            </div>
            <div class="field">
                <label>Blog Content</label>
                <textarea name="blog[body]"></textarea>
            </div>

            <button class="ui violet basic big button" type="submit">Post</button>
        </form>
    </div>


    <% include partials/footer %>
